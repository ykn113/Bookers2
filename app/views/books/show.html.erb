<h1>Books#show</h1>
<p>Find me in app/views/books/show.html.erb</p>

<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
        <tr>
          <td>
            <%= attachment_image_tag @user, :profile_image, size:"50x50", fallback: "no_image.jpg"%></br>
            <%= link_to @book.user.name, user_path(params[:id]) %>
          </td>
          <td><%= link_to @book.title, book_path(params[:id]) %></td>
          <td><%= @book.body %></td>
          <td><%= link_to "Edit", edit_book_path, class:"btn btn-sm btn-success" %></td>
          <td>
            <% if @book.user==current_user %>
                <%= link_to "Destroy", book_path(@book),method: :delete, class:"btn btn-sm btn-danger" %>
            <% end %>
          </td>
        </tr>
      </table>
    </div>
  </div>
</div>